<h:outputScript>
    window.addEventListener('load', function() {
        var selected = document.querySelector('.ui-datatable .ui-state-highlight');
        if (selected) {
            selected.scrollIntoView({ block: 'center' });
        }
    });
</h:outputScript>
