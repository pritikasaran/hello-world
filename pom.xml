public void loadFirstEntry() {
        if (postkorbModell.getEintraege() != null && !postkorbModell.getEintraege().isEmpty()) {
            firstEntry = postkorbModell.getEintraege().get(0).toString();
        } else {
            firstEntry = "No entries";
        }
    }
